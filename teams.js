let teams=window.FMX_DEMO.teams;function draw(){const q=(FMX.$('teamSearch').value||'').toLowerCase();const list=teams.filter(x=>x.team.name.toLowerCase().includes(q));FMX.$('allTeams').innerHTML=list.map(x=>`<a class="club-card" href="equipo.html?id=${x.team.id}"><span class="arrow">↗</span>${x.team.logo?`<img src="${FMX.esc(x.team.logo)}" alt="Escudo ${FMX.esc(x.team.name)}">`:`<span class="logo-fallback">${FMX.initials(x.team.name)}</span>`}<b>${FMX.esc(x.team.name)}</b><small>${FMX.esc(x.venue?.name||'Ficha del club')}</small></a>`).join('')||'<div class="empty">No encontramos ese club.</div>'}FMX.$('teamSearch').addEventListener('input',draw);FMX.setDataState(false,window.FMX_MANUAL?.updatedAt);draw();
