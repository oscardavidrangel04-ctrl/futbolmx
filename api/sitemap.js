export default function handler(req,res){const host=(process.env.SITE_URL||`https://${req.headers.host}`).replace(/\/$/,'');const pages=['/','/partidos.html','/tabla.html','/equipos.html','/simulador.html'];const now=new Date().toISOString().slice(0,10);const xml=`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${pages.map(p=>`\n<url><loc>${host}${p}</loc><lastmod>${now}</lastmod><changefreq>${p==='/'?'daily':'daily'}</changefreq><priority>${p==='/'?'1.0':'0.8'}</priority></url>`).join('')}\n</urlset>`;res.setHeader('Content-Type','application/xml; charset=utf-8');res.setHeader('Cache-Control','s-maxage=3600');res.status(200).send(xml)}
